<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example.collaborative_editor.mapper.DocumentMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into document (doc_id, title, content, owner_id, status, create_time, update_time, create_user, update_user)
        values (#{docId}, #{title}, #{content}, #{ownerId}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <update id="update">
        update document
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        where doc_id = #{docId}
    </update>

</mapper>
